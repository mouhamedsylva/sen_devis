import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales (mode clair)
  static const Color primary = Color(0xFF1A7B7B);
  static const Color primaryDark = Color(0xFF1A7B7B);
  static const Color secondary = Color(0xFF000000);
  static const Color background = Color(0xFFFFFFFF);
  
  // Couleurs de texte (mode clair)
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textOnPrimary = Color(0xFF000000);
  
  // Couleurs d'état
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);
  
  // Couleurs pour les statuts de devis
  static const Color statusDraft = Color(0xFF9E9E9E);
  static const Color statusSent = Color(0xFF2196F3);
  static const Color statusAccepted = Color(0xFF4CAF50);
  
  // Couleurs d'interface (mode clair)
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFBDBDBD);
  static const Color disabled = Color(0xFFBDBDBD);
  
  // Ombres
  static const Color shadow = Color(0x1A000000);
  
  // ==================== MODE SOMBRE ====================
  
  // Couleurs principales (mode sombre)
  static const Color darkPrimary = Color(0xFF1A7B7B);
  static const Color darkSecondary = Color(0xFF4DB8B8);
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color darkCard = Color(0xFF2C2C2C);
  
  // Couleurs de texte (mode sombre)
  static const Color darkTextPrimary = Color(0xFFFFFFFF);
  static const Color darkTextSecondary = Color(0xFFB0B0B0);
  static const Color darkTextOnPrimary = Color(0xFF000000);
  
  // Couleurs d'interface (mode sombre)
  static const Color darkBorder = Color(0xFF3A3A3A);
  static const Color darkDivider = Color(0xFF3A3A3A);
  static const Color darkDisabled = Color(0xFF4A4A4A);
  
  // Ombres (mode sombre)
  static const Color darkShadow = Color(0x40000000);
}
